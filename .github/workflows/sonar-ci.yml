name: CI + Sonar 🚀

on:
  pull_request:
    branches: ["main", "test-ci"]
    paths:
      - "backend/**"
      - "frontend/**"
      - "sonar-project.properties"
      - ".github/workflows/ci-sonar.yml"
  

jobs:
  backend:
    name: Backend CI 🐍
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: ludokan
          POSTGRES_USER: ludokan
          POSTGRES_PASSWORD: ludokan
        ports: ["5432:5432"]
        options: >-
          --health-cmd="pg_isready -U ludokan -d ludokan"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      SECRET_KEY: test-secret-key
      DEBUG: "True"
      DATABASE_URL: postgres://ludokan:ludokan@localhost:5432/ludokan
      REDIS_URL: redis://localhost:6379/0
      CELERY_BROKER_URL: redis://localhost:6379/1

    steps:
      - name: Checkout 📦
        uses: actions/checkout@v4

      - name: Set up Python 🐍
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install backend dependencies 📥
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run migrations 🗄️
        working-directory: ./backend
        run: python manage.py migrate --noinput

      - name: Black 🎨
        working-directory: ./backend
        run: black . --check --diff

      - name: isort 📚
        working-directory: ./backend
        run: isort . --check-only --diff

      - name: Ruff 🐕
        working-directory: ./backend
        run: ruff check .

      - name: flake8 🔍
        working-directory: ./backend
        run: flake8 .

      - name: Pytest + coverage.xml 🧪
        working-directory: ./backend
        run: pytest --cov=. --cov-report=xml:coverage.xml

      - name: Upload backend coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage
          path: backend/coverage.xml

  frontend:
    name: Frontend CI ⚛️
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout 📦
        uses: actions/checkout@v4

      - name: Use Node.js 📦
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "./frontend/package-lock.json"

      - name: Install dependencies 📦
        run: npm ci

      - name: ESLint 🔍
        run: npm run lint

      - name: Prettier (format check) 🎨
        run: npm run format:check

      - name: TypeScript check 🧠
        run: npm run type-check

      # ✅ Si tu as des tests frontend qui génèrent coverage/lcov.info, active ce step
      # - name: Tests + coverage (lcov)
      #   run: npm run test -- --coverage --watch=false
      #   env:
      #     CI: "true"

      - name: Build ⚙️
        run: npm run build

      # ✅ Si tu actives les tests au-dessus, décommente aussi cet upload
      # - name: Upload frontend coverage artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: frontend-coverage
      #     path: frontend/coverage

  sonar:
    name: SonarCloud Scan 🔎
    runs-on: ubuntu-latest
    needs: [backend, frontend]

    steps:
      - name: Checkout (full history) 📦
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download backend coverage
        uses: actions/download-artifact@v4
        with:
          name: backend-coverage
          path: backend

      - name: Verify Sonar secrets are set
        shell: bash
        run: |
          if [ -z "${{ secrets.SONAR_TOKEN }}" ]; then
            echo "❌ SONAR_TOKEN is empty or not available"
            exit 1
          fi
          if [ -z "${{ secrets.SONAR_ORG }}" ]; then
            echo "❌ SONAR_ORG is empty or not available"
            exit 1
          fi
          if [ -z "${{ secrets.SONAR_PROJECT_KEY }}" ]; then
            echo "❌ SONAR_PROJECT_KEY is empty or not available"
            exit 1
          fi
          echo "✅ Sonar secrets are present"

      # ✅ Si tu actives la couverture frontend, décommente ces 2 blocs :
      # - name: Download frontend coverage
      #   uses: actions/download-artifact@v4
      #   with:
      #     name: frontend-coverage
      #     path: frontend/coverage

      - name: Run SonarCloud scan
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=${{ secrets.SONAR_ORG }}
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.host.url=https://sonarcloud.io
