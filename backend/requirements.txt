Django==4.2.30
djangorestframework==3.15.2
django-filter==23.3
django-notifications-hq==1.8.2
wheel>=0.46.2
django-import-export[all]==4.4.0

# Authentification
django-allauth==0.57.0
dj-rest-auth==5.0.2
djangorestframework-simplejwt==5.5.1  
dj-rest-auth[with_jwt]==5.0.2


# CORS
django-cors-headers==4.3.1

# Database & configuration
# psycopg2-binary==2.9.7
psycopg2-binary==2.9.10
python-decouple==3.8
dj-database-url==2.1.0  

# Images
Pillow==12.2.0  # Fix GHSA-whj4-6x5x-4v2j (HIGH)

# Storage (S3-compatible: Cloudflare R2)
django-storages[s3]==1.14.2
boto3==1.34.0

# Celery & Redis
celery==5.3.4
redis==5.0.1
django-redis==5.4.0  # pour brancher Redis comme cache Django

# WebSockets / Channels
channels==4.1.0
channels-redis==4.2.0

# ASGI server (pour servir HTTP + WebSockets)
daphne==4.1.0

# Dev / utilitaires (optionnel)
whitenoise==6.5.0  # servir les statiques en prod sans nginx
drf-spectacular==0.27.2  # docs OpenAPI/Swagger
gunicorn==23.0.0


# HTTP client (reCAPTCHA siteverify)
requests>=2.31.0,<3

# Logging
sentry-sdk[django]==2.20.0

# Runtime tooling (pkg_resources used by rest_framework_simplejwt; removed in setuptools 82+)
setuptools>=75.0.0,<82.0.0
jaraco.context>=6.1.0  # GHSA-58pv-8j8x-9vj2 (transitive via setuptools)

# transitions for state tickets
django-fsm==3.0.1

# Export PDF des rapports (BACK-021E)
reportlab>=4.0.0